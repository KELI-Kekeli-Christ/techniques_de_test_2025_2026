def compute_triangulation(points: list[tuple[float, float]]) -> list[tuple[int, int, int]]:
    """
    Calcule la triangulation d'un ensemble de points.
    
    Args:
        points: Liste de tuples (x, y).
        
    Returns:
        Liste de tuples (i, j, k) représentant les indices
        des points formant un triangle.
    """
    # Logique à implémenter pour le Rendu 3
    raise NotImplementedError("compute_triangulation")